#include <stdio.h>

int main()
{
    // int len = 4;
    // int wid = 6;
    // printf("the area of rectange is %d\n", len * wid);
    // return 0;

    int len, wid;
    printf("Enter the length\n");
    scanf("%d", &len);
    printf("Enter the width\n");
    scanf("%d", &wid);
    printf("the area of rectange is %d\n", len * wid);
}