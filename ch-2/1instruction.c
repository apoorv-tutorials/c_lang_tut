#include <stdio.h>

int main()
{
    // int a = 4; // type declaration instruction
    // int d = 4, b, c;
    // b = c = a ;
    // printf("the vlue of b and c is %d %d\n", b, c);
    float a = 1.1;
    float b = a + 8.9;
    printf("the value of b is %f\n", b);
    return 0;
}